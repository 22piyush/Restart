<!-- Categories Of DataTypes -->
Primitive Types :- number , string , boolean , null , undefined, null, bigint, symbol;

Object Types :- Array, Tuples;

Special Types :- any, unknown, void, never;

Advanced Types :- union, intersection, type alias, enum, literal Types

Function Types :- Define The function Types 
                let add: (x: number , y: number) => number;
                add = (x, y) => x + y;

